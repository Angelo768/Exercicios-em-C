#include <stdio.h>
#include <stdlib.h>

int main(){

    // int i, *n = (int*)calloc(6, sizeof(int));
    // for(i=0;i<5;i++) printf("Sei lá %d\n", *n);//scanf("%d", &(*n[5]));
    // int i, **t, **v2 = NULL;
    int a = 0;
    a = getchar();
    printf("%d\n", a);
}
