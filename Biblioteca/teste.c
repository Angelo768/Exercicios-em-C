#include <stdio.h>
#include <stdlib.h>

int main(){

    // int i, *n = (int*)calloc(6, sizeof(int));
    // for(i=0;i<5;i++) printf("Sei lá %d\n", *n);//scanf("%d", &(*n[5]));
    // int i, **t, **v2 = NULL;
    int v[] = {5, 10, 30, 20};
    printf("%d\n", sizeof(v)/sizeof(int));

}
